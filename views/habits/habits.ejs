<%- include('../partials/header')  %> 

<div class="container">
  <br>
  <br>
  <div class="row">
    <table class="table" id="main-tb">
      <thead>
        <tr>
          <th scope="col">Delete</th>
          <th scope="col">Streak</th>
        </tr>
      </thead>
      <tbody>
        <% habits.forEach(h => { %> 
          <tr>
            <td scope="row"><a href="/habits/<%= h._id %>"></a><%= h.habit %></a></td>
            <td scope="row"><%= h.streak %> </td>
          </tr>
        <% }); %> 
      </tbody>
    </table>
  </div>
</div>

<%- include('../partials/footer')  %> 